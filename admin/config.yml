backend:
  name: git-gateway
  branch: main

media_folder: "images/uploads"
public_folder: "/images/uploads"

collections:
  - name: "pages"
    label: "Pages"
    files:
      - label: "Top Page"
        name: "top"
        file: "index.html"
        fields:
          - { label: "Body", name: "body", widget: "text" }